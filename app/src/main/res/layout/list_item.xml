<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_item_height"
    android:background="?attr/selectableItemBackground"
    android:clickable="true">

    <ImageView
        android:layout_width="@dimen/circleradius"
        android:layout_height="@dimen/circleradius"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="@dimen/circlemargin"
        android:layout_marginRight="@dimen/circlemargin"
        android:background="@drawable/ic_record"/>

    <TextView
        android:id="@+id/list_item_title"
        android:layout_width="wrap_content"
        android:layout_weight="5"
        android:layout_height="match_parent"
        android:maxLines="1"
        android:ellipsize="end"
        android:gravity="left|center"
        android:textSize="@dimen/textsizebig"/>

    <TextView
        android:id="@+id/list_item_time"
        android:layout_width="25dp"
        android:layout_weight="1"
        android:layout_height="match_parent"
        android:layout_margin="20dp"
        android:textColor="@color/myGrayTextColor"
        android:maxLines="1"
        android:ellipsize="end"
        android:gravity="right"
        android:layout_gravity="right|top"
        android:textSize="@dimen/textsizesmall"/>

</LinearLayout>